- hosts: localhost
  roles:
  - role: standard-test-basic
    tags:
      - classic
    required_packages:
      - libomp-test
    tests:
      - libomp-regression-tests:
          dir: ./
          run: /usr/share/libomp/runtest.sh --threads 8
